// export const PROTOCOL = "http"; // This is back-end server protocol
// export const PORT = 5000; // This is back-end server port
// export const HOST_NAME = location.hostname; // This is back-end hostname. Front-end and back-end hostnames are same usually.

//export const BackEndWebServer = "https://www.xyzeki.com" 
// const BackEndWebServer = `https://${location.hostname}`
export const BackEndWebServer = `http://localhost:5000`
// `${PROTOCOL}://${HOST_NAME}:${PORT}/`;