import { SortByDateTimePipe } from './sort-by-date-time.pipe';

describe('SortByDateTimePipe', () => {
  it('create an instance', () => {
    const pipe = new SortByDateTimePipe();
    expect(pipe).toBeTruthy();
  });
});
