import { LiAnimateDirective } from './li-animate.directive';

describe('LiAnimateDirective', () => {
  it('should create an instance', () => {
    const directive = new LiAnimateDirective();
    expect(directive).toBeTruthy();
  });
});
